# main.tf — Пример для локальной инфраструктуры

# Используем локальный provider (не создает никаких облачных ресурсов)
provider "local" {}

# Пример — создадим инфофайл при инициализации (можно заменить на реальные задачи)
resource "local_file" "info" {
  filename = "${path.module}/info.txt"
  content  = "Учебный проект: стартовый пример работы Terraform локально."
}

# (Здесь же позже могут быть ресурсы для запуска VM, настройки сети и т.д.)
